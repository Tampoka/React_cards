import React from 'react';
import spinner from '../../../assets/icons/1480.gif'

const Spinner = () => {
    return (
        <div>
            <img src={spinner} alt='spinner'/>
        </div>
    );
};

export default Spinner;